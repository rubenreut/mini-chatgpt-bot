/**
 * This file is a redirect to maintain backward compatibility
 * with components that haven't been migrated yet.
 */

import { ChatProvider, useChatContext } from '../features/chat/context/ChatContext';

export { ChatProvider, useChatContext };
export default { ChatProvider, useChatContext };