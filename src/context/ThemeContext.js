/**
 * This file is a redirect to maintain backward compatibility
 * with components that haven't been migrated yet.
 */

import { ThemeProvider, useThemeContext } from '../features/theme/context/ThemeContext';

export { ThemeProvider, useThemeContext };
export default { ThemeProvider, useThemeContext };